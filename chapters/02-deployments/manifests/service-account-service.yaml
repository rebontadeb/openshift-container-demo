apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: financeflow-workshop
  labels:
    app: financeflow
    tier: account-service
spec:
  selector:
    tier: account-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
