apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: financeflow-portal
  namespace: myfinance-demo
  labels:
    app: financeflow
    tier: portal
spec:
  lookupPolicy:
    local: true
